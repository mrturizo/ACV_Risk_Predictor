# Streamlit Web App Requirements
# Para deploy en Streamlit Cloud
# NOTA: El modelo lr_pca25_cw.pkl es un Pipeline de sklearn que se carga con joblib,
# NO requiere PyCaret para su uso (solo para entrenamiento). PyCaret se eliminó para
# evitar conflictos de dependencias (numpy, scikit-learn, sktime).

streamlit>=1.28.0
pandas>=1.5.0,<2.2.0
numpy>=1.21.0,<1.26.0  # Compatible con scikit-learn 1.2.0-1.4.0
scikit-learn>=1.2.0,<1.4.0  # Compatible con el modelo entrenado
joblib>=1.3.0  # Necesario para cargar el modelo .pkl
plotly>=5.14.0
reportlab>=3.6.0
fpdf
openpyxl>=3.0.0

